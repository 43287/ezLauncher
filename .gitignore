# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Node
node_modules

# Build outputs
dist
dist-ssr

# Rust / Cargo build output (任意层级的 crate，覆盖 src-tauri/target 与子 crate 如 rdev_test/target)
target/
# Tauri 自动生成的 capability schema
src-tauri/gen/schemas

# TypeScript 增量编译缓存
*.tsbuildinfo

# Environment
*.local
.env
.env.local
.env.*.local

# Trae AI specs and context
.trae/
specs/
.specify/

# Claude Code 本地工具产物（CLAUDE.md 不在此列，应被跟踪）
.claude/

# Docs and Output
output/
docs/

# 运行时数据（DPAPI 密文，含个人应用列表，不应进仓库）
**/data/apps.json
**/data/settings.json
**/data/*.bak
**/data/*.tmp
# 运行时图标缓存（按机器生成，非源码）
**/data/icon/cache/

# Editor directories and files
.vscode/
.idea
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
